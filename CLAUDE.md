# CLAUDE.md — aspget プロジェクトルール

このファイルはClaude Codeが毎回読み込む。**ここに書かれたルールは機能要件より優先される。**

---

## プロジェクト概要

3社のASP（バリューコマース / A8.net / もしもアフィリエイト）から案件情報を日次収集し、
差分（新着・報酬変更・終了）を検知してLINEに通知する社内ツール。

詳細仕様は `docs/IMPLEMENTATION_SPEC.md` を参照すること。

---

## 最重要原則

### 1. ASPアカウントの凍結を絶対に避ける

システムが止まる損失より、ASPアカウントを失う損失のほうが圧倒的に大きい。
**収集の網羅性より、アクセスの慎ましさを常に優先すること。**
「効率化のため」という理由でアクセス頻度を上げる変更を提案してはならない。

### 2. HTMLを解析せず、CSVをダウンロードする

管理画面のHTML構造は予告なく変わるが、CSVの列構成はめったに変わらない。
ブラウザ自動化は「画面を読む」のではなく「ダウンロードボタンを押す」ためだけに使う。
一覧のHTMLをパースする実装は原則禁止。

### 3. 推測で実装しない

仕様書の `【要実測】` 箇所（APIエンドポイント、DOMセレクタ、CSV列名、文字コード）は
**必ず実際のドキュメント・実画面を確認してから確定させる。**
それらしいコードを書いて動いたことにするのは、本プロジェクトで最悪の失敗である。
確認できない場合は実装を止めて質問すること。

### 4. ASPアダプタを完全に分離する

1社の失敗が全体を止めてはならない。各アダプタの呼び出しは個別にtry-exceptで囲む。

### 5. 生データを捨てない

ダウンロードしたCSV、APIレスポンスは `storage/raw/{asp}/{YYYY-MM-DD}/` に原本を保存する。
パースにバグが見つかった際、ASPへ再アクセスせずに再処理できるようにするため。

---

## 禁止事項

1. 複数ASPへの並列アクセス（必ず直列・1社ずつ）
2. 1日2回以上の収集実行
3. CAPTCHA・追加認証の自動突破（出現したら中断して人間に通知）
4. 案件の自動提携申請
5. ログイン失敗後の連続リトライ（最大1回まで。アカウントロックを招く）
6. `【要実測】` 箇所の推測による実装
7. 認証情報・セッションファイル・生データのGitコミット
8. 収集データの外部への再配布・公開
9. 認証情報やトークンのログ出力

---

## ブラウザ動作ルール（変更禁止）

| 項目 | 設定値 |
|---|---|
| 実行頻度 | 1日1回まで |
| 並列実行 | 禁止 |
| 実行時刻 | 深夜〜早朝JST、ASPごとに30分以上ずらす |
| 画面遷移間ウェイト | `random.uniform(2, 5)` 秒 |
| リトライ | 最大3回、指数バックオフ（30s → 120s → 300s） |
| 3回失敗時 | 中断してLINE通知。自動再試行を続けない |

---

## 実装順序（厳守）

```
Step 0: 基盤（DB接続・マイグレーション・LINE疎通）
Step 1: バリューコマース（API方式）← ここで土台を完成させる
Step 2: A8.net（Playwright + CSV DL）
Step 3: もしもアフィリエイト（Step 2を継承）
```

**各Stepの受け入れ基準を満たすまで次のStepへ進まない。** 並行実装しない。

---

## 技術スタック

- Python 3.11+
- Playwright（Chromium, headless）
- PostgreSQL（外部ホスティング）
- タイムゾーンは **JST（Asia/Tokyo）で統一**。UTCで保存する場合も、日付判定は必ずJSTで行う

---

## コーディング規約

- CSVの列は**位置ではなくヘッダ名で参照する**。マッピングは `config/column_mapping.yaml` に外出し
- 文字コードはUTF-8決め打ちにしない（Shift-JISの可能性が高い）
- 期待する列が見つからない場合は**例外を投げて止める**。推測で近い列を使わない
- 報酬額のパースに失敗しても例外を投げず、`reward_raw` に原文を残してNULLで通す
- 正規化ロジックはアダプタ内に書かず `normalizer.py` に集約する

---

## 開発時の注意

- 開発中は必ず `DRY_RUN=true` で動作確認する（DB書き込み・LINE通知を行わない）
- テスト実行のたびに本番のASPへアクセスしない。保存済みの生データを使って再処理すること
